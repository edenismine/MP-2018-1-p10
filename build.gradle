apply plugin: 'java'
apply plugin: 'application'
mainClassName = 'mx.unam.fciencias.myp.PlayDemo'
javadoc{
    options.memberLevel = JavadocMemberLevel.PRIVATE
}
jar {
    manifest {
        attributes(
                'Class-Path': configurations.compile,
                'Main-Class': mainClassName
        )
    }
}